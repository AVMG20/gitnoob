{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the gitnoob window needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
