{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the gitnoob window needs. The tab strip is the title bar, and dragging one moves the window \u2014 the one thing core:default stops short of allowing.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-start-dragging"
  ]
}
