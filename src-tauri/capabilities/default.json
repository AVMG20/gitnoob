{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the gitui window needs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
