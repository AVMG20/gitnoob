{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the gitnoob window needs. The tab strip is the title bar, and dragging one moves the window — the one thing core:default stops short of allowing. The updater checks for, downloads and installs a signed release, and restarting is what finishes the job.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-start-dragging",
    "updater:default",
    "process:allow-restart"
  ]
}
